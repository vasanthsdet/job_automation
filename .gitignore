# Secrets & credentials — never commit these
.env
linkedin_cookies.json

# Job tracker — generated fresh per run, downloaded as artifact from GitHub Actions
applied_jobs.csv
last_reset_date.txt

# Logs
*.log
task_scheduler.log
scheduler.log

# Resume (stored as GitHub Secret — not in repo)
*.docx
*.pdf

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# Windows
Thumbs.db
desktop.ini
run_job_bot.bat
